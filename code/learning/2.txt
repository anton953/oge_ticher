📌 Тип задания:
Восстановить сообщение, закодированное неравномерным кодом (обычно условие Фано).


🔍 Шаг 1: Внимательно прочитать условие
Дана таблица кодов букв (или их нужно вывести из дерева).

Дана закодированная двоичная последовательность.

Нужно её однозначно декодировать.


🧩 Шаг 2: Убедиться, что код удовлетворяет условию Фано
Это значит: ни один код не является началом другого кода. Это гарантирует однозначность декодирования.


✂️ Шаг 3: Последовательное декодирование (метод "от начала")
Берём первые несколько битов закодированной строки.

Сравниваем их с кодами из таблицы.

Как только нашли совпадение — записываем букву, а эти биты отрезаем от строки.

Повторяем с оставшимися битами.

Важно: Можно делать прямо под строкой, последовательно подписывая буквы.

✅ Шаг 4: Проверить результат
Все биты должны быть использованы.

Полученная строка должна состоять только из букв, коды которых есть в таблице.


🎯 Итоговая памятка для задания №2:
🔍 Начни с проверки условия Фано (в уме).

✂️ Декодируй последовательно с первого бита.

📝 Под строкой пиши найденные буквы, зачёркивая использованные биты.

✅ Все биты должны кончиться — это проверка.

⚠️ Если зашёл в тупик — вернись на шаг назад, возможно, взял не ту длину кода (редко, но бывает).

Лайфхак: Иногда дают дерево кодов — просто иди от корня по веткам (0 — влево, 1 — вправо), пока не придёшь к букве.

