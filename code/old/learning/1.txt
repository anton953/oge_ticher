📌 Тип задания:
Оценить объём памяти, необходимый для хранения текстовой или графической информации.

🔍 Шаг 1: Определить, с чем работаем (текст или изображение)
📝 Текст: Объём = кол-во символов × вес одного символа.

🖼️ Изображение (растр): Объём = кол-во пикселей × вес одного пикселя (глубина цвета).

Вес символа в кодировке Unicode (UTF-8 или UTF-16) часто равен 2 байта (16 бит), если не сказано иное.
Вес пикселя зависит от глубины цвета: например, 16 цветов = 4 бита (2⁴=16).

🧮 Шаг 2: Выписать все данные из условия и перевести в одинаковые единицы
Найти общее количество (символов или пикселей).

Узнать информационный вес одного (в битах или байтах).

Все расчёты вести в битах, а потом переводить в байты/Кбайты (1 байт = 8 бит, 1 Кбайт = 1024 байта).

✏️ Шаг 3: Подставить в формулу и посчитать
Объём (в битах)
=
Количество
×
Вес одного
Объём (в битах)=Количество×Вес одного
Перевести в требуемые в ответе единицы (чаще всего Кбайты).

⚠️ Шаг 4: Проверить "подвохи"
Различать степени двойки: 1 Кбайт = 1024 байта, а не 1000.

Внимательно читать, сколько страниц, сколько строк, сколько символов в строке.

Для графики: разрешение = ширина × высота (пикселей).

📊 Пример задания (Текст):
«Статья содержит 48 страниц, на каждой странице 36 строк, в каждой строке 48 символов. Сколько Кбайт займёт статья в кодировке Unicode (16 бит на символ)?»

Решение:

📝 Это текст → формула: Объём = кол-во символов * 2 байта (т.к. 16 бит = 2 байта).

🧮 Найдём общее число символов:

Символов на странице = 36 строк × 48 символов = 1728 симв.

Во всей статье = 1728 × 48 страниц = 82 944 символа.

✏️ Объём в байтах = 82 944 × 2 байта = 165 888 байт.

Переведём в Кбайты: 165 888 / 1024 = 162 Кбайт.

✅ Ответ: 162 Кбайт.

🎨 Пример задания (Изображение):
«Рисунок размером 512×128 пикселей сохранили как 8-цветное изображение. Каков размер файла в Кбайтах?»

Решение:

🖼️ Это графика. 8 цветов → глубина цвета: 2³ = 8 → 3 бита на пиксель.

🧮 Всего пикселей = 512 × 128 = 65 536.

✏️ Объём в битах = 65 536 × 3 бита = 196 608 бит.

Переведём в байты (делим на 8): 196 608 / 8 = 24 576 байт.

В Кбайты (делим на 1024): 24 576 / 1024 = 24 Кбайт.

✅ Ответ: 24 Кбайт.

🎯 Итоговая памятка:
📝 Текст → символы × 2 байта (если Unicode-16).

🖼️ Графика → ширина × высота × глубина цвета (в битах).

🧮 Считать аккуратно, переводить единицы через 1024, а не 1000.

✅ Проверить, в чём просят ответ (байты, Кбайты).