📌 Тип задания:
Дана схема дорог между пунктами (часто с односторонним движением). Нужно по таблице восстановить количество дорог между пунктами или найти пункт по описанию.

🔍 Шаг 1: Внимательно изучить условие
Есть пункты (обычно A, B, C, D, E...).

Есть дороги (стрелки) между ними.

Важно: Дороги могут быть однонаправленными (→) или двусторонними (↔).

Дана таблица, где на пересечении строки и столбца стоит число дорог из пункта строки в пункт столбца.

Правило:

На пересечении строки X и столбца Y стоит число дорог из X в Y.

Если дорог нет — ставим 0.

Если дорога двусторонняя, она даёт +1 в обе клетки: (X→Y) и (Y→X).

🧩 Шаг 3: Типы вопросов
«Сколько всего дорог?» — просуммировать все числа в таблице.

«Сколько дорог ведёт в пункт K?» — сумма столбца K.

«Сколько дорог выходит из пункта K?» — сумма строки K.

«Между какими пунктами нет дорог?» — ищем нули в таблице (кроме диагонали).

«Какой пункт имеет наибольшее число исходящих дорог?» — сравниваем суммы строк.

📝 Шаг 4: Алгоритм решения
Нарисуйте таблицу по условию.

Заполните её, анализируя схему:

Если есть стрелка из A в B → +1 в клетку (A, B).

Если дорога двусторонняя → +1 в (A, B) и +1 в (B, A).

Ответьте на вопрос, используя таблицу.

📊 Пример задания (классический):
Между населёнными пунктами A, B, C, D, E построены дороги, протяжённость которых (в километрах) приведена в таблице:

Примечание: Знак «-» означает, что дороги между пунктами нет.

Вопрос: Определите длину кратчайшего пути между пунктами A и C.

Решение:

🔍 Задача уже дана в табличной форме (здесь длины, а не количество дорог).

🧭 Нужен кратчайший путь от A до C. Прямой дороги нет (в таблице на (A, C) стоит «-»).

🛣️ Ищем возможные маршруты:

A → B → C: 4 + 1 = 5 км.

A → D → E → C: 5 + 3 + 6 = 14 км (длиннее).

A → D → B → C: 5 + 3 + 1 = 9 км (длиннее).

A → B → E → C: 4 + 2 + 6 = 12 км (длиннее).

✅ Кратчайший: A → B → C = 5 км.

Ответ: 5.

🎯 Итоговая памятка для задания №4:
🗺️ Нарисуй таблицу сразу, даже если её нет в условии — так нагляднее.

🔁 Различай направление: строка → столбец.

➕ Двусторонняя дорога = +1 в обе клетки.

🧮 Сумма строки = сколько дорог исходит из пункта.

🧮 Сумма столбца = сколько дорог входит в пункт.

🔍 Если спрашивают протяжённость пути — ищи все возможные маршруты, считай сумму.

⚠️ Не путай таблицу количества дорог и таблицу длин дорог (вторую нельзя просто суммировать для числа дорог).

Лайфхак: Если в задаче про количество дорог и дана схема — просто перенеси стрелки в таблицу, ставь 1 вместо каждой стрелки.

